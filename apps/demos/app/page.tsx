export default function DemosCatalogPage() {
  return (
    <main>
      <h1>Demos</h1>
      <p>Catalog + iframe host — proxy routes forward to DEMO_PROXY_BASE when configured.</p>
    </main>
  );
}
