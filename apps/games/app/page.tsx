export default function GamesLobbyPage() {
  return (
    <main>
      <h1>Games</h1>
      <p>Game library / lobby — Colyseus sidecar runs as a separate process (see server/).</p>
    </main>
  );
}
