/**
 * Colyseus (or other WS) game server — run as a separate Node process / Docker sidecar.
 * Example: `pnpm exec tsx server/index.ts` from apps/games (wire rooms + Colyseus when ready).
 */
console.info("[games/server] Placeholder — add Colyseus Server + rooms here.");
